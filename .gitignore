/docs/
/lib/
/bin/
/.shards/
*.dwarf

# Libraries don't need dependency lock
# Dependencies will be locked in applications that use them
/shard.lock

# A local override for where termbuf-input is checked out: a path, and so
# nobody else's.
/shard.override.yml

!/CLAUDE.md
/scratch/
/.spectator-failures
/tmp/
/.rumdl_cache/

# Compiled examples: crystal build drops the binary beside its source.
/examples/clock
/examples/validate
/scripts/__pycache__/
